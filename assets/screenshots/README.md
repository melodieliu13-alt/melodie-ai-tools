# Screenshot assets

Reserved file for the remaining KOL proof:

- `kol-exporter-live-run.png`

The image will be added only after a real Chrome-extension run on a public X account. It must show:

- the exporter running on the public account page;
- a completed status;
- the new or cumulative item count.

The final crop must exclude browser avatars, bookmarks, signed-in account details and other private information.

Planned caption:

> KOL Signal exporter completing a real public-account run, with completion status and the new or cumulative item count visible.

No placeholder, simulated screenshot or broken Markdown image link should be committed before the real image exists.
